package bean.documents;

public class CDs extends ADocument {

    public CDs(int idDocument, String title, String auteur, Boolean isDisponible, int dateSorti) {
        super(idDocument, title, auteur, isDisponible, dateSorti);
    }

}
